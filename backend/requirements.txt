fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.4.0
pydantic-settings>=2.0.0
# LangChain 1.x 核心包（与旧版 0.x API 不兼容，必须锁定大版本）
langchain>=1.0.0,<2.0.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.3.0
langchain-ollama>=0.3.0
langchain-text-splitters>=0.3.0
# langchain-classic：LangChain 1.x 把部分 legacy 类迁入此包保持兼容
# ContextualCompressionRetriever / CrossEncoderReranker 等均在此包
langchain-classic>=1.0.0
# RAG 模块依赖
chromadb>=0.6.0
langchain-chroma>=0.2.0
langchain-huggingface>=0.1.0
sentence-transformers>=3.0.0
pypdf>=4.0.0
python-multipart>=0.0.9
httpx>=0.27.0
rapidocr_onnxruntime>=1.3.24
# 长期记忆 / 持久化 Checkpointer 依赖
# psycopg v3（异步版）：AsyncPostgresSaver 和 PGVector 均依赖此驱动
psycopg[binary,pool]>=3.1.0
# LangGraph 官方 Postgres Checkpointer，负责多轮对话历史持久化
langgraph>=0.3.0
langgraph-checkpoint-postgres>=2.0.0
# LangChain 官方 Postgres 向量库封装（PGVector），用于长期语义记忆
langchain-postgres>=0.0.12
